#include <stdio.h>

int main() {
  float a, b, c, d, sin1, sin2;

  printf ("Donner moi un réels a :");
  scanf ("%f", &a);

  printf ("Donner moi un réels b :");
  scanf ("%f", &b);

  }
  for (a = a ; a <= b ; a = a + 0.1){
	printf ("%1.1f\n", c);
	sin = a - ((a * a * a) / 6) + ((a * a * a * a * a) / 120) + ((a * a * a * a * a * a * a) / 5040);
  }
  for (a = -3 ; a <= 3 ; a = a + 0.5){
	printf ("%1.1f|", a);
	sin = a - ((a * a * a) / 6) + ((a * a * a * a * a) / 120) + ((a * a * a * a * a * a * a) / 5040);
  	for (b = 0
  }
}
 
